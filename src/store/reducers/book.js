import { ADD_BOOK } from "../consts"

const initialState = {
    book: {},
    myBooks: []
}

export default function(state = initialState, action) {
    
    switch (action.type) {
        
        default:
           return state
    }
}