import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        margin: 20,
    },
    label: {
        color: '#fff',
        fontWeight: 'bold',
        fontSize: 18,
    },
    text: {
        fontSize: 18,
    }
})