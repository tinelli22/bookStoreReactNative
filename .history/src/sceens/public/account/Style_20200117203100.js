import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        display: 'flex',
        justifyContent: 'space-around',
        alignItems: 'center',
    },
    label: {
        color: '#fff',
        fontWeight: 'bold',
        fontSize: 18,
    },
    text: {
        fontSize: 18,
    }
})